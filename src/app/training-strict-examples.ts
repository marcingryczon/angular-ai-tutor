// strict: false
// Kod, który przypisuje null do zmiennej stringowej
// odkomentuj i uruchom npm start

/**
let name:string = null
name.toUpperCase();

// Klasa z niezainicjowanym polem
class Person {
  name:string;
}

new Person();

// Funkcja bez typów parametrów (ukryte any)
function add(a,b) {
  return a+b;
}

add(1,2);

// Funkcja, która nie zwraca wartości we wszystkich ścieżkach

function divide(a, b) {
  if(b === 0) {
    return 'Cannot divide by 0';
  }
}
*/